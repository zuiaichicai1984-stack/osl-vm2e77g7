"use strict";
/**
 * Viem entry point for the OpenSea SDK.
 *
 * @example
 * ```ts
 * import { createPublicClient, createWalletClient, http } from 'viem'
 * import { mainnet } from 'viem/chains'
 * import { OpenSeaSDK, Chain } from '@opensea/sdk/viem'
 *
 * const publicClient = createPublicClient({ chain: mainnet, transport: http() })
 * const walletClient = createWalletClient({ chain: mainnet, transport: http(), account: '0x...' })
 *
 * const sdk = new OpenSeaSDK(
 *   { publicClient, walletClient },
 *   { chain: Chain.Mainnet, apiKey: 'YOUR_API_KEY' },
 * )
 * ```
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __exportStar = (this && this.__exportStar) || function(m, exports) {
    for (var p in m) if (p !== "default" && !Object.prototype.hasOwnProperty.call(exports, p)) __createBinding(exports, m, p);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.OpenSeaSDK = exports.OpenSeaAPI = void 0;
// seaport-js has a hard dependency on ethers, so importing it pulls ethers
// into the bundle. This is a known tradeoff — see FAQ for details.
const seaport_js_1 = require("@opensea/seaport-js");
const api_1 = require("./api/api");
const seaport_bridge_1 = require("./provider/seaport-bridge");
const viem_adapter_1 = require("./provider/viem-adapter");
const base_1 = require("./sdk/base");
const types_1 = require("./types");
const address_1 = require("./utils/address");
const utils_1 = require("./utils/utils");
// Re-export everything from the main entry point
var api_2 = require("./api/api");
Object.defineProperty(exports, "OpenSeaAPI", { enumerable: true, get: function () { return api_2.OpenSeaAPI; } });
__exportStar(require("./api/types"), exports);
__exportStar(require("./api/walletAuth"), exports);
__exportStar(require("./auth"), exports);
__exportStar(require("./constants"), exports);
__exportStar(require("./orders/types"), exports);
__exportStar(require("./types"), exports);
__exportStar(require("./utils"), exports);
/**
 * OpenSea SDK for use with viem.
 *
 * Accepts viem `PublicClient` and optional `WalletClient` instead of ethers types.
 * The public API surface is identical to the ethers entry point.
 *
 * @category Main Classes
 */
class OpenSeaSDK extends base_1.BaseOpenSeaSDK {
    /**
     * Create a new instance of OpenSeaSDK using viem clients.
     * @param viemClients The viem PublicClient and optional WalletClient
     * @param apiConfig Configuration options, including `chain`
     * @param logger Optional function for logging debug strings
     */
    constructor(viemClients, apiConfig = {}, logger) {
        apiConfig.chain ??= types_1.Chain.Mainnet;
        const chain = apiConfig.chain;
        const api = new api_1.OpenSeaAPI(apiConfig);
        const resolvedLogger = logger ?? ((arg) => arg);
        const wallet = (0, viem_adapter_1.createViemWallet)(viemClients);
        const contractCaller = (0, viem_adapter_1.createViemContractCaller)(viemClients);
        // Create ethers-compatible signer/provider for seaport-js
        const seaportSignerOrProvider = (0, seaport_bridge_1.createSeaportBridge)(viemClients, resolvedLogger);
        const defaultConduit = (0, utils_1.getDefaultConduit)(chain);
        const seaportAddress = (0, utils_1.getSeaportAddress)(chain);
        const seaport = new seaport_js_1.Seaport(seaportSignerOrProvider, {
            conduitKeyToConduit: {
                [defaultConduit.key]: defaultConduit.address,
            },
            overrides: {
                defaultConduitKey: defaultConduit.key,
                contractAddress: seaportAddress,
            },
        });
        const cachedPaymentTokenDecimals = {};
        cachedPaymentTokenDecimals[(0, utils_1.getOfferPaymentToken)(chain).toLowerCase()] = 18;
        cachedPaymentTokenDecimals[(0, utils_1.getListingPaymentToken)(chain).toLowerCase()] = 18;
        super({
            wallet,
            contractCaller,
            seaport,
            api,
            chain,
            logger: resolvedLogger,
            getAvailableAccounts: async () => {
                if ("signer" in wallet) {
                    try {
                        const address = await wallet.signer.getAddress();
                        return [(0, address_1.checksumAddress)(address)];
                    }
                    catch {
                        return [];
                    }
                }
                return [];
            },
            cachedPaymentTokenDecimals,
        });
    }
}
exports.OpenSeaSDK = OpenSeaSDK;
//# sourceMappingURL=viem.js.map
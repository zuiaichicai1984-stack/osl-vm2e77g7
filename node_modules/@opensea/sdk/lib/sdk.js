"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.OpenSeaSDK = void 0;
const seaport_js_1 = require("@opensea/seaport-js");
const api_1 = require("./api/api");
const ethers_adapter_1 = require("./provider/ethers-adapter");
const base_1 = require("./sdk/base");
const types_1 = require("./types");
const utils_1 = require("./utils/utils");
/**
 * The OpenSea SDK main class (ethers entry point).
 *
 * @example
 * ```ts
 * import { ethers } from 'ethers'
 * import { OpenSeaSDK, Chain } from '@opensea/sdk'
 *
 * const provider = new ethers.JsonRpcProvider('https://mainnet.infura.io')
 * const sdk = new OpenSeaSDK(provider, { chain: Chain.Mainnet, apiKey: 'YOUR_KEY' })
 * ```
 *
 * @category Main Classes
 */
class OpenSeaSDK extends base_1.BaseOpenSeaSDK {
    /**
     * Create a new instance of OpenSeaSDK.
     * @param signerOrProvider Signer or provider to use for transactions.
     * @param apiConfig configuration options, including `chain`
     * @param logger optional function for logging debug strings. defaults to no logging
     */
    constructor(signerOrProvider, apiConfig = {}, logger) {
        apiConfig.chain ??= types_1.Chain.Mainnet;
        const chain = apiConfig.chain;
        const api = new api_1.OpenSeaAPI(apiConfig);
        const resolvedLogger = logger ?? ((arg) => arg);
        const wallet = (0, ethers_adapter_1.createEthersWallet)(signerOrProvider);
        const contractCaller = (0, ethers_adapter_1.createEthersContractCaller)(signerOrProvider);
        const defaultConduit = (0, utils_1.getDefaultConduit)(chain);
        const seaportAddress = (0, utils_1.getSeaportAddress)(chain);
        const seaport = new seaport_js_1.Seaport(signerOrProvider, {
            conduitKeyToConduit: {
                [defaultConduit.key]: defaultConduit.address,
            },
            overrides: {
                defaultConduitKey: defaultConduit.key,
                contractAddress: seaportAddress,
            },
        });
        const cachedPaymentTokenDecimals = {};
        cachedPaymentTokenDecimals[(0, utils_1.getOfferPaymentToken)(chain).toLowerCase()] = 18;
        cachedPaymentTokenDecimals[(0, utils_1.getListingPaymentToken)(chain).toLowerCase()] = 18;
        super({
            wallet,
            contractCaller,
            seaport,
            api,
            chain,
            logger: resolvedLogger,
            getAvailableAccounts: () => (0, ethers_adapter_1.getEthersAccounts)(signerOrProvider, resolvedLogger),
            cachedPaymentTokenDecimals,
        });
    }
}
exports.OpenSeaSDK = OpenSeaSDK;
//# sourceMappingURL=sdk.js.map
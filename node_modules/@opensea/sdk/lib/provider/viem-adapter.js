"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createViemWallet = createViemWallet;
exports.createViemContractCaller = createViemContractCaller;
/**
 * Viem adapter — wraps viem WalletClient/PublicClient into the SDK's abstract interfaces.
 *
 * viem is an optional peerDependency and a devDependency (for type-checking).
 * This module is only executed at runtime by code importing from "@opensea/sdk/viem",
 * which requires viem to be installed.
 */
const viem_1 = require("viem");
/**
 * Creates an OpenSeaWallet from viem clients.
 */
function createViemWallet(params) {
    const provider = createViemProvider(params.publicClient);
    if (params.walletClient) {
        return {
            signer: createViemSigner(params.walletClient, params.publicClient),
            provider,
        };
    }
    return { provider };
}
function createViemSigner(walletClient, publicClient) {
    return {
        async getAddress() {
            if (!walletClient.account) {
                throw new Error("WalletClient must have an account. Use `createWalletClient` with an `account` parameter.");
            }
            return walletClient.account.address;
        },
        async sendTransaction(tx) {
            if (!walletClient.account) {
                throw new Error("WalletClient must have an account. Use `createWalletClient` with an `account` parameter.");
            }
            const hash = await walletClient.sendTransaction({
                to: tx.to,
                data: tx.data,
                value: tx.value,
                account: walletClient.account,
                chain: walletClient.chain,
                ...tx.overrides,
            });
            return {
                hash,
                async wait() {
                    await publicClient.waitForTransactionReceipt({ hash });
                },
            };
        },
        async signTypedData(domain, types, value) {
            if (!walletClient.account) {
                throw new Error("WalletClient must have an account. Use `createWalletClient` with an `account` parameter.");
            }
            return walletClient.signTypedData({
                domain: {
                    chainId: Number(domain.chainId),
                    name: domain.name,
                    version: domain.version,
                    verifyingContract: domain.verifyingContract,
                },
                types,
                primaryType: Object.keys(types).find(key => key !== "EIP712Domain") ||
                    Object.keys(types)[0],
                message: value,
                account: walletClient.account,
            });
        },
    };
}
function createViemProvider(publicClient) {
    return {
        async waitForTransaction(hash) {
            await publicClient.waitForTransactionReceipt({
                hash: hash,
            });
        },
    };
}
/**
 * Creates a ContractCaller using viem clients.
 */
function createViemContractCaller(params) {
    const { publicClient, walletClient } = params;
    return {
        async readContract({ address, abi, functionName, args }) {
            return publicClient.readContract({
                address: address,
                abi: abi,
                functionName,
                args: args,
            });
        },
        async writeContract({ address, abi, functionName, args, value, overrides, }) {
            if (!walletClient) {
                throw new Error("A WalletClient is required for write operations");
            }
            if (!walletClient.account) {
                throw new Error("WalletClient must have an account. Use `createWalletClient` with an `account` parameter.");
            }
            const hash = await walletClient.writeContract({
                address: address,
                abi: abi,
                functionName,
                args: args,
                value,
                account: walletClient.account,
                chain: walletClient.chain,
                ...overrides,
            });
            return {
                hash,
                async wait() {
                    await publicClient.waitForTransactionReceipt({
                        hash: hash,
                    });
                },
            };
        },
        encodeFunctionData({ abi, functionName, args }) {
            return (0, viem_1.encodeFunctionData)({
                abi: abi,
                functionName,
                args: args,
            });
        },
    };
}
//# sourceMappingURL=viem-adapter.js.map